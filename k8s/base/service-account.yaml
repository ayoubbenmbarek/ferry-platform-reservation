apiVersion: v1
kind: ServiceAccount
metadata:
  name: maritime-sa
  namespace: maritime-reservations
  labels:
    app.kubernetes.io/name: maritime-reservations
---
# Optional: If using AWS EKS with IAM roles for service accounts (IRSA)
# Uncomment and configure the annotation below
# apiVersion: v1
# kind: ServiceAccount
# metadata:
#   name: maritime-sa
#   namespace: maritime-reservations
#   annotations:
#     eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/maritime-reservations-role
